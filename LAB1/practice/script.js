// List 4 data types in a multiline comment
/* 
Integer 
boolean 
string 
float 
*/
// Typeof
console.log(typeof 14);
console.log(typeof true);
// Let vs Const vs Var
let brr = 3939;
brrr = 9393
 
const krr = "B)"
//krr = "O^O"

// Reassignment (no let)
aura= 100
aura = 1000
aura = 10000
// Essential Operations
brr += brrr
console.log(brr);
brr -= 3
console.log(brr)
let brrrr = brr/brrr
console.log(brrrr);
brrrr *= brr
console.log(brrrr);
 brrrr /= brr
 console.log(brr);
// Additional operations

// User Input
const shh = prompt( "what is your social security?")
console.log (" ss is "+ shh)
// Concatenation v. templates

// String Methods (uppercase, lowercase, includes)

// Math methods

// Comparison operators - true or false?

// Conditional statements;

// Functions

// Returns half the number if it can be halved; other``wise returns false

// Functions can also be written as arrows. Body of function doesn't change, just the signature.
